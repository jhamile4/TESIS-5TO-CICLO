# Copia este archivo como .env y completa tus valores
# cp .env.example .env

# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tu_password_aqui
DB_NAME=crecio_db

# JWT — cámbialo por cualquier texto secreto largo
JWT_SECRET=crecio_jwt_secret_cambia_esto

# Puerto del servidor
PORT=3001
